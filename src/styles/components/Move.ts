import { createStyles } from '@mantine/core';

export const useMoveStyles = createStyles(() => ({
  wrapper: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'flex-start',
    padding: '1rem 0',
    width: 'fit-content',
    gap: '0.25rem',
  },
}));
